# Create conda environment via:
#   conda create -n huggingface_playground python=3 spacy ftfy pytorch pytorch-pretrained-bert ignite tensorboardx=1.6  blas=*=mkl -c pytorch -c conda-forge
# NOTE: tensorboardx > 1.6 breaks ignite=0.2.0
#
# To use openai-gpt model, install spacy model for tokenization:
#   python -m spacy download en
#
# To use gpt2 model, do not install pytorch-pretrained-bert via conda, but install its branch "attention":
#   git clone https://github.com/huggingface/pytorch-pretrained-BERT.git
#   cd pytorch-pretrained-BERT
#   git checkout attention
#   pip install .
#
# Eventually, install Apex to speed up training/lower resource consumption (FP16, distributed, ...),
# e.g. "--fp16 O1" is required to train+evaluate gpt2@GPU \w 11GB memory:
#   git clone https://github.com/NVIDIA/apex
#   cd apex
#   pip install -v --no-cache-dir --global-option="--cpp_ext" --global-option="--cuda_ext" ./
#
#########################################################################################


torch
pytorch-ignite
pytorch-pretrained-bert >= 0.6.2
tensorboardX
tensorflow  # for tensorboardX
flask # for interact endpoint