# Create conda environment via:
#   conda create -n huggingface_playground python=3 spacy ftfy pytorch pytorch-pretrained-bert ignite tensorboardx=1.6  blas=*=mkl -c pytorch -c conda-forge
# NOTE: tensorboardx > 1.6 breaks ignite=0.2.0
#
# To use gpt2 model, do not install pytorch-pretrained-bert via conda, but install its branch "gpt-2-special-tokens" from my fork:
#   git clone https://github.com/ArneBinder/pytorch-pretrained-BERT
#   cd pytorch-pretrained-BERT
#   git checkout origin/gpt-2-special-tokens
#   pip install .


torch
pytorch-ignite
pytorch-pretrained-bert >= 0.6.2
tensorboardX
tensorflow  # for tensorboardX