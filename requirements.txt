# Create conda environment via:
#   conda create -n huggingface_playground python=3 spacy ftfy pytorch pytorch-pretrained-bert ignite tensorboardx=1.6  blas=*=mkl -c pytorch -c conda-forge
# ATTENTION: tensorboardx > 1.6 breaks ignite=0.2.0
#
# THIS DOES NOT WORK, FIX IT!
# To use gpt2 model, do not install pytorch-pretrained-bert via conda, but:
#   git clone https://github.com/huggingface/pytorch-pretrained-BERT.git
#   <merge branch origin/gpt-2-special-tokens to master>
#   pip install .


torch
pytorch-ignite
pytorch-pretrained-bert >= 0.6.2
tensorboardX
tensorflow  # for tensorboardX